// Entorno de producción
export const environment = {
    production: true,
    // apiUrl: 'https://api.google.com/api',
    // apiUrl: 'https://warmhelp-back.onrender.com/api', // aquí pones la URL del backend en Render
    apiUrl: 'https://warmhelp-production.up.railway.app/api', // backend deploy
    tokenSecure: true,
}